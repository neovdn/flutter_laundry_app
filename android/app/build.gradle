plugins {
    id "com.android.application"
    id "kotlin-android"
    id "com.google.gms.google-services" // Firebase Plugin (FlutterFire)
    id "dev.flutter.flutter-gradle-plugin" // Flutter Plugin
}

android {
    namespace = "com.example.flutter_laundry_app"
    compileSdk = 34 // Pastikan ini sesuai dengan versi SDK yang kamu install
    ndkVersion = "25.1.8937393"

    defaultConfig {
        applicationId = "com.example.flutter_laundry_app"
        minSdkVersion 23
        targetSdk = 34 // Pastikan sesuai dengan compileSdk
        versionCode = 1
        versionName = "1.0.0"
    }

    buildTypes {
        release {
            signingConfig signingConfigs.debug // Pastikan debug signingConfig memang ada
            minifyEnabled false
            shrinkResources false
        }
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }

    kotlinOptions {
        jvmTarget = "17"
    }
}

flutter {
    source = "../.."
}
